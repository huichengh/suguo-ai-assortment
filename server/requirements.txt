# 苏果智选 · 后台服务依赖
#
# 设计取舍说明：
# 1) 密码哈希用 bcrypt 原生库，不用 passlib —— passlib 1.7.4 与 bcrypt 4.x
#    存在已知的 __about__ 属性兼容缺陷，绕开可避免踩坑。
# 2) JWT 用 PyJWT，接口比 python-jose 直接，依赖更少。
# 3) 算法层用 pandas/numpy 重写，与前端 algos.js 的纯函数实现保持一致。

fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.6
PyJWT>=2.9
bcrypt>=4.2
python-multipart>=0.0.12
pandas>=2.2
numpy>=1.26
python-dateutil>=2.9

# 测试
pytest>=8.3
httpx>=0.27
